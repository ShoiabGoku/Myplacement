import { ModulePage } from "@/components/modules/ModulePage";

export default function AerospacePage() {
  return (
    <ModulePage
      module="aerospace"
      tagline="GATE, ISRO, DRDO, HAL, Airbus, Boeing — fluid mechanics to flight control, at every difficulty."
    />
  );
}
