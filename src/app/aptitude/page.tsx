import { ModulePage } from "@/components/modules/ModulePage";

export default function AptitudePage() {
  return (
    <ModulePage
      module="aptitude"
      tagline="Quant, logical reasoning, verbal and DI — the screening rounds every company throws at you."
    />
  );
}
